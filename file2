$users = Get-ChildItem C:\Users|Where-Object{$_.name -notlike '*Default*' -and $_.name -notlike '*PUblic*'}
foreach ($user in $users){
Remove-Item "$($user.fullname)\AppData\Local\Google\Chrome" -Force -Recurse
   }
